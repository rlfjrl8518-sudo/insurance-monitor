"""NVIDIA NIM API로 광고 이미지+텍스트를 분석해 소재유형/보종/소구포인트/요약으로 분류하는 모듈."""

import json

분류_결과_키 = ["소재유형", "보종", "소구포인트", "요약"]

# NVIDIA NIM은 OpenAI 호환 엔드포인트라 openai SDK로 그대로 호출한다.
NVIDIA_기본_URL = "https://integrate.api.nvidia.com/v1"

# 소재유형 분류 규칙.
# 브랜딩 항목은 실제 라벨과 대조해 다시 쓴 것이다. 원래는 '믿을 수 있는/업계 1위' 같은
# 키워드만 나열돼 있었는데, 실제로 브랜딩으로 분류되는 소재는 정보성 콘텐츠·이벤트·
# 스폰서십·채널 홍보가 대부분이라 키워드가 하나도 걸리지 않아 전부 기타로 샜다.
# 실제 분포 150건 대조에서 소재유형 일치율 73.3% -> 86.7% (브랜딩 35% -> 96%).
소재유형_분류_규칙 = """[소재유형 분류 기준]

비갱신
- 정의: 보험료 인상 없음, 비갱신형 특징을 강조하는 소재
- 대표 키워드: 비갱신, 보험료 인상 없는, 만기까지 동일 보험료, 갱신 걱정 없는, 처음 보험료 그대로
- 제외: 특약 설명만 있는 경우, 브랜드 소개만 있는 경우

특약
- 정의: 구체적인 보장 내용 또는 특약을 강조하는 소재
- 포함 조건 (아래 중 하나라도 해당하면 특약):
  1. 특약명이 명시된 경우
     예) 암진단특약, 입원일당특약, 수술비특약 등
  2. 담보명이 포함된 경우
     예) 간병인사용일당, 후유장해, 운전자 벌금, 변호사 선임비, 배상책임 등
  3. 담보 내용이 구체적으로 설명된 경우
     예) "골절 시 OO만원 지급", "입원 1일당 OO원"
  4. "(특약)" 문구가 광고 텍스트에 포함된 경우
  5. 특정 보장이 금액·조건과 함께 강조된 경우
     예) "수술비 첫날부터 보장", "진단비 OO만원"
- 대표 키워드: 암진단, 입원일당, 수술비, 간병인, 후유장해, 운전자 벌금, 변호사 선임비, 배상책임
- 중요: 상품명만 언급하고 구체적인 담보명·금액·보장조건이 없으면 특약이 아니라 브랜딩이다.
  예) "OO생명 100세암치료보장보험 지금 확인해 보세요" -> 브랜딩
      "암 진단 시 5,000만원 지급" -> 특약
- 제외: 비갱신이 핵심 메시지인 경우

브랜딩
- 정의: 특정 담보·보장을 앞세우지 않고, 회사/브랜드/채널로 관심과 유입을 만드는 소재 전반
- 포함 조건 (아래 중 하나라도 해당하면 브랜딩):
  1. 회사·브랜드 이미지나 신뢰도 강조
     예) 믿을 수 있는, 고객 만족, 가입자 수, 업력, 업계 1위
  2. 보험 상품과 직접 관련 없는 정보성·생활정보 콘텐츠
     예) 금융상식, 계절 건강관리 팁, 반려동물 정보, 여행/드라이브 코스 소개
  3. 이벤트·경품·응모·체험 캠페인
     예) 선물 증정 이벤트, 참가자 모집, 추첨 경품
  4. 스포츠단·유튜브·블로그·SNS 등 자사 채널이나 스폰서십 홍보
  5. 진단·비교·점검 같은 자사 서비스나 도구로 유입시키는 소재
     예) 우리집 안전 진단, 보험 점검 서비스, 보험료 비교
  6. 상품명이나 상품 라인업만 제시하고 구체적 담보 내용이 없는 소재
     예) "OO생명 종신보험", "통합보험V2", "지금 바로 확인해 보세요"
- 제외: 구체적 담보명·보장금액이 핵심 메시지인 경우(-> 특약),
        보험료가 오르지 않는다는 점이 핵심인 경우(-> 비갱신)

기타
- 위 3개에 모두 해당하지 않는 경우에만 사용한다.
- 광고주가 보험사이고 자사 브랜드·채널·상품을 알리는 내용이라면 대부분 브랜딩에 해당하므로,
  기타로 분류하기 전에 브랜딩 조건을 다시 확인하라. 기타는 드물게 나와야 한다.

판단 우선순위: 비갱신 > 특약 > 브랜딩 > 기타
단, 비갱신 + 특약이 동시에 있는 경우 -> 비갱신 우선"""

# 보종 분류 규칙 (우선순위 순서대로 적용)
보종_분류_규칙 = """[보종 분류 기준] - 우선순위 순서대로 적용

1순위 여성보험 (가장 높음)
- 키워드: 여성, 유방암, 자궁, 난소, 갑상선, 여성질환
- 주의: "임신 중 여성을 위한 보장" -> 태아보험 X, 여성보험 O

2순위 태아보험
- 키워드: 태아, 출산준비, 신생아
- 제외: 임신 여성 건강보장 -> 여성보험으로

3순위 간병인보험
- 키워드: 간병, 간병인, 돌봄

4순위 주택화재
- 키워드: 화재, 누수, 우리집, 주택

5순위 골프플랜
- 키워드: 골프, 홀인원

6순위 보장분석
- 키워드: 보장점검, 보험점검, 보험리모델링, 보장분석

7순위 어린이보험
- 키워드: 어린이, 어린이보험, 영유아, 아이보험, 태아, 자녀

8순위 암보험
- 키워드: 암, 암보험, 암진단, 항암, 암치료

9순위 운전자보험
- 키워드: 운전자, 운전자보험, 벌금, 면허, 교통사고, 변호사 선임비

10순위 치아보험
- 키워드: 치아, 치아보험, 충전, 임플란트, 스케일링, 크라운

11순위 펫보험
- 키워드: 펫, 반려동물, 강아지, 고양이, 펫보험

12순위 자동차보험
- 키워드: 자동차, 자동차보험, 차보험, 이륜차, 오토바이

13순위 여행보험
- 키워드: 여행, 여행보험, 해외여행, 항공, 비행기

14순위 종합보험
- 키워드: 종합보험, 건강보험, 종합보장, 통합보장, 질병, 입원, 수술, 의료비
- 정의: 특정 보종에 한정되지 않는 일반 건강/질병 보장을 다루는 소재

15순위 기타 (가장 낮음)
- 규칙: 위 보종 중 어느 것에도 해당하지 않을 경우 최종 분류

참고: 목록에는 있지만 위에 순위/키워드 규칙이 없는 보종(구글시트 "설정" 탭에서 새로 추가된
경우)이 있다면, 카테고리명 자체와 광고 텍스트에서 가장 명확하게 언급된 상품/보장 키워드를
근거로 최선을 다해 판단하라."""

# 소구포인트 분류 규칙 - "무엇을 강조하는가"(메시지 내용)를 기준으로 판단.
# 실제 라벨과 대조해 다시 쓴 것이다. 원래 규칙으로는 상품신뢰가 17%밖에 안 잡혔는데,
# 보험 광고 대부분이 말미에 "확인해 보세요" 같은 문구를 달고 있어 우선순위가 더 높은
# 가입유도가 매번 먼저 걸렸기 때문이다. 상투적 CTA는 근거로 보지 말라고 명시했다.
# 실제 분포 130건 대조에서 52.3% -> 63.1% (상품신뢰 17% -> 52%).
#
# 남은 과제: 이 규칙은 가입유도를 과하게 조여서 82% -> 33%로 떨어뜨렸다.
# "마감·한정이 없어도 가입 권유가 본문의 대부분이면 가입유도"로 푸는 안을 준비했으나
# NVIDIA 무료 티어 할당량이 소진돼 검증하지 못했다. 할당량이 회복되면 재측정할 것.
소구포인트_분류_규칙 = """[소구포인트 분류 기준] - "무엇을 강조하는가"를 기준으로 판단
판단 우선순위: 시즌이슈 > 정서유도 > 가입유도 > 경제적혜택 > 대비유도 > 상품신뢰

중요 - 상투적 CTA는 근거가 아니다:
거의 모든 보험 광고는 말미에 "확인해 보세요", "알아보기", "상담 받기", "가입하기"
같은 문구가 붙는다. 이건 형식적인 꼬리말이지 광고의 메시지가 아니다.
이런 문구가 있다는 이유만으로 가입유도로 분류하지 마라.
앞쪽 본문이 무엇을 말하고 있는지로 판단하라.

시즌이슈
- 정의: 특정 계절·시기성 이슈와 결부된 소구
- 키워드: 장마, 태풍, 누수, 폭염, 한파, 독감, 환절기, 신학기, 연말정산, 여름철, 겨울철
- 예시: 여름철 화재/누수 대비, 장마철 집 안전점검, 겨울철 독감 대비

정서유도
- 정의: 감성·공감·가족애가 메시지의 앞면에 오는 소재
- 포함: 육아·반려동물·가족을 향한 애정이나 걱정을 이야기하듯 풀어내는 소재,
        선물·축하·설렘 같은 정서를 앞세운 이벤트
- 키워드: 즐겁게, 행복, 마음 편히, 소중한 가족, 우리 아이, 울 애기, 걱정되는 마음
- 예시: "지친 하루 당신의 퇴근길", "첫 운전 선물", 화자의 경험담·후기 형식

가입유도
- 정의: 지금 행동하라는 촉구 자체가 메시지의 중심인 경우
- 포함 조건: 마감·한정·기간·즉시성이 걸려 있어야 한다
- 키워드: 지금 가입, 한정, 마감 임박, 선착순, 오늘까지, 혜택 소진 전, 서둘러
- 제외: 본문이 보장 내용이나 가격을 설명하고 끝에 "확인해 보세요"만 붙은 경우

경제적혜택
- 정의: 보험료 절감·환급·저렴함·사은품 등 금전적 이득이 핵심
- 키워드: 보험료, 저렴, 할인, 환급, 인상 없는, 상품권, 페이백, 카드결제혜택
- 참고 경향: 소재유형=비갱신인 소재에서 자주 나타남

대비유도
- 정의: 특정 위험·상황이 닥칠 수 있으니 미리 대비하라는 메시지
- 키워드: 대비, 준비, 만약, 갑작스러운, 위험, 혹시 모를, 예기치 못한
- 예시: "갑작스러운 사고에 대비하세요", "병원비 부담이 현실로 다가올 때"

상품신뢰
- 정의: 보장 내용·스펙·한도의 충실함, 또는 브랜드·상품 자체의 신뢰성을 내세우는 경우
- 포함 조건 (아래 중 하나라도 해당하면 상품신뢰):
  1. 보장 항목·한도·범위를 나열하며 "든든하다/충분하다"를 강조
     예) "파손 수리비 90% 보상", "든든해진 보장 한도", "주요 보험 스펙"
  2. 상품이 새로워졌거나 개선됐음을 알림
     예) "새롭게 돌아온 NEW OO보험", "보장이 더 좋아졌어요"
  3. 브랜드 신뢰도·업력·점유율 강조
     예) 믿을 수 있는, 업계 1위, 가입자 수, 대한민국 1위
- 주의: 위 1~3에 해당하면, 끝에 "확인해 보세요" 같은 문구가 있어도 가입유도가 아니라
        상품신뢰다. 상품신뢰는 마지막 순위지만 실제로 자주 쓰이는 분류다."""

# ── 확장 분류 축 ───────────────────────────────────────────────────────────
# 지금 쓰는 세 축(소재유형/보종/소구포인트)만으로는 "어떤 조합이 자주 같이 쓰이나"
# 같은 분석을 할 수가 없다. 조합이 나올 만큼 축이 많지 않아서다.
#
# 축을 둘로 나눈다. 레이아웃이나 인물 유무는 텍스트만 봐서는 알 수 없고, 반대로
# 퍼널 단계나 오퍼는 이미지만 봐서는 알기 어렵다. 그래서 이미지를 이미 보고 있는
# 비전 단계에서 시각 축을, 텍스트 분류 단계에서 메시지 축을 각각 판단한다.
# 호출 횟수는 그대로다.

시각축 = {
    "레이아웃": ["인물 중앙", "제품 중심", "텍스트 중심", "분할 구성", "기타"],
    "인물노출": ["있음", "없음"],
    "제품노출": ["뚜렷함", "보조적", "없음"],
    "텍스트밀도": ["낮음", "보통", "높음"],
    "제작방식": ["실사 촬영", "일러스트", "타이포그래피", "3D·그래픽", "혼합"],
    "비주얼스타일": ["라이프스타일", "프로모션", "정보전달", "브랜드", "기타"],
}

메시지축 = {
    "퍼널단계": ["인지", "관심", "전환"],
    "오퍼": ["이벤트", "할인", "사은품", "무료상담", "없음"],
    "긴급성": ["마감 임박", "한정", "없음"],
}


def _축_안내(축정의):
    return "\n".join(f"- {이름}: {' / '.join(값들)} 중 하나" for 이름, 값들 in 축정의.items())


확장자별_MIME = {
    ".jpg": "image/jpeg",
    ".jpeg": "image/jpeg",
    ".png": "image/png",
    ".webp": "image/webp",
}


def 분류_프롬프트_생성(광고주, 광고텍스트, 설정):
    """config.json의 분류 기준을 반영한 AI 프롬프트를 생성한다.

    규칙 텍스트(우선순위/키워드 정의)는 구글시트 "분류규칙" 탭에서 사용자가 직접
    수정할 수 있다 (설정_동적_적용이 설정["classification_rules"]에 채워 넣음).
    시트에 값이 없거나 시트 접근 전(로컬 테스트 등)에는 이 파일의 기본 규칙을 쓴다.
    """
    분류기준 = 설정["classification"]
    규칙_오버라이드 = 설정.get("classification_rules", {})
    소재유형_규칙 = 규칙_오버라이드.get("소재유형") or 소재유형_분류_규칙
    보종_규칙 = 규칙_오버라이드.get("보종") or 보종_분류_규칙
    소구포인트_규칙 = 규칙_오버라이드.get("소구포인트") or 소구포인트_분류_규칙

    소재유형_목록 = " or ".join(분류기준["소재유형"])
    보종_목록 = " or ".join(분류기준["보종"])
    소구포인트_목록 = " or ".join(분류기준["소구포인트"])

    return f"""당신은 보험 광고 소재 분류 전문가입니다.
아래 기준에 따라 광고 텍스트와 이미지를 분석하여
소재유형 / 보종 / 소구포인트 / 요약을 분류하세요.

출력은 반드시 JSON 형식으로만 출력하세요.
설명, 마크다운 없이 JSON만 출력하세요.

---

{소재유형_규칙}

---

{보종_규칙}

---

{소구포인트_규칙}

---

[분류 판단 순서]

1. 텍스트에서 핵심 메시지가 무엇인지 파악
2. 소재유형 분류 (비갱신 > 특약 > 브랜딩 > 기타 순으로 체크)
3. 보종 분류 (우선순위 순서대로 체크)
4. 소구포인트 분류 ("무엇을 강조하는가" 관점에서 판단)
5. 요약 작성 (핵심 메시지 25자 이내 한 문장)

---

[입력]
광고주: {광고주}
광고텍스트:
{광고텍스트}

(이미지가 함께 제공되지 않은 경우 광고텍스트만으로 최선을 다해 분류하세요.)

---

[출력 형식]
{{
  "소재유형": "{소재유형_목록}",
  "보종": "{보종_목록}",
  "소구포인트": "{소구포인트_목록}",
  "요약": "핵심 메시지 25자 이내"
}}
"""


def _분류결과_검증(결과, 설정):
    """분류 결과가 허용 목록에 없는 경우 "기타"로 대체한다."""
    분류기준 = 설정["classification"]
    if 결과.get("소재유형") not in 분류기준["소재유형"]:
        결과["소재유형"] = "기타"
    if 결과.get("보종") not in 분류기준["보종"]:
        결과["보종"] = "기타"
    if 결과.get("소구포인트") not in 분류기준["소구포인트"]:
        결과["소구포인트"] = "기타"
    결과["요약"] = 결과.get("요약", "")
    # 확장 축은 허용값이 아니면 아예 담지 않는다. 빈 값이 섞이면 집계할 때 걸러내야 한다.
    for 이름, 값들 in {**시각축, **메시지축}.items():
        if 이름 in 결과 and 결과[이름] not in 값들:
            결과.pop(이름)
    return 결과


def 모델_생성(설정):
    """NVIDIA NIM 클라이언트를 만든다 (OpenAI 호환 엔드포인트라 openai SDK를 그대로 쓴다)."""
    from openai import OpenAI

    nv = 설정["nvidia"]
    # 무료 티어는 용량이 차면 503(Worker limit reached)을 자주 돌려주는데 잠시 뒤
    # 재시도하면 대개 통과하므로 SDK 기본 재시도(2회)보다 넉넉히 준다.
    return OpenAI(api_key=nv["api_key"],
                  base_url=nv.get("base_url", NVIDIA_기본_URL),
                  max_retries=nv.get("max_retries", 4))


def _호출옵션(설정):
    """분류 호출에 붙는 공통 파라미터.

    thinking=False: deepseek-v4-flash는 기본적으로 눈에 안 보이는 추론 토큰을 쏟아낸다.
    실측상 끄면 건당 23.9초 -> 6.2초, 출력 326토큰 -> 60토큰이고 정확도는 떨어지지 않았다
    (오히려 소구포인트가 조금 나아졌다). 모델을 바꿀 때는 이 값이 유효한지 다시 재 볼 것.
    temperature=0: 같은 광고를 다시 돌렸을 때 분류가 흔들리는 것을 줄인다.
    max_tokens: 추론을 켠 상태의 실측 최대가 1,008토큰이라 여유를 둔다.
    timeout: 무료 티어는 같은 호출이 3초~120초를 오가므로 넉넉히 잡는다.
    """
    nv = 설정["nvidia"]
    옵션 = {
        "max_tokens": nv.get("max_tokens", 2000),
        "temperature": 0,
        "timeout": nv.get("timeout", 150),
        "response_format": {"type": "json_object"},
    }
    if nv.get("thinking", False) is False:
        옵션["extra_body"] = {"chat_template_kwargs": {"thinking": False}}
    return 옵션


def _JSON_파싱(텍스트):
    """response_format을 무시하고 ```json 펜스나 앞뒤 설명을 붙여 보내는 모델까지 처리한다."""
    텍스트 = (텍스트 or "").strip()
    try:
        return json.loads(텍스트)
    except json.JSONDecodeError:
        시작, 끝 = 텍스트.find("{"), 텍스트.rfind("}")
        if 시작 == -1 or 끝 <= 시작:
            raise
        return json.loads(텍스트[시작:끝 + 1])


def _이미지_문구_읽기(client, 이미지_경로, 설정):
    """vision_model로 이미지 속 문구만 뽑아낸다. 비어 있으면 None.

    분류를 잘하는 모델과 이미지를 빠르게 읽는 모델이 서로 다르기 때문에 단계를 나눈다.
    (분류 모델에 이미지를 직접 물리면 건당 90초를 넘어갔고, 전용 비전 모델에 OCR만
    시키면 3초대에 끝난다.)
    """
    import base64

    nv = 설정["nvidia"]
    확장자 = "." + 이미지_경로.rsplit(".", 1)[-1].lower()
    mime_type = 확장자별_MIME.get(확장자, "image/jpeg")
    with open(이미지_경로, "rb") as f:
        이미지_base64 = base64.b64encode(f.read()).decode()

    확장 = nv.get("extended_labels", False)
    if 확장:
        # 이미 이미지를 보고 있으니 시각 축도 같이 받는다. 호출을 늘리지 않기 위해서다.
        지시 = (
            "이미지를 보고 아래 JSON만 출력하라. 설명 금지.\n"
            '{"문구": "이미지에 적힌 문구를 그대로", '
            + ", ".join(f'"{이름}": ""' for 이름 in 시각축)
            + "}\n\n각 항목은 아래 중 하나만 고른다:\n"
            + _축_안내(시각축)
        )
    else:
        지시 = "이미지에 적힌 문구만 그대로 옮겨 적어라. 설명·해석·반복 금지."

    응답 = client.chat.completions.create(
        model=nv["vision_model"],
        messages=[{
            "role": "user",
            "content": [
                {"type": "image_url",
                 "image_url": {"url": f"data:{mime_type};base64,{이미지_base64}"}},
                {"type": "text", "text": 지시},
            ],
        }],
        max_tokens=600 if 확장 else 300,
        temperature=0,
        timeout=nv.get("vision_timeout", 60),
    )
    본문 = (응답.choices[0].message.content or "").strip()
    if not 본문:
        return None, {}
    if not 확장:
        return 본문, {}

    # 확장 모드에서 JSON이 깨져도 OCR 문구는 건지는 게 낫다.
    try:
        d = _JSON_파싱(본문)
    except Exception:
        return 본문, {}
    시각값 = {이름: d.get(이름) for 이름 in 시각축 if d.get(이름) in 시각축[이름]}
    return (d.get("문구") or "").strip() or None, 시각값


def _이미지문구_합치기(광고텍스트, 이미지문구):
    if not 이미지문구:
        return 광고텍스트
    return f"{광고텍스트}\n\n[이미지에서 읽은 문구]\n{이미지문구}"


def 광고_분류_텍스트전용(client, 광고주, 광고텍스트, 설정):
    """이미지 없이 광고 텍스트만으로 분류한다. 실패 시 예외를 발생시킨다."""
    프롬프트 = 분류_프롬프트_생성(광고주, 광고텍스트, 설정)
    if 설정["nvidia"].get("extended_labels", False):
        프롬프트 += (
            "\n\n[추가 판단 항목] - 위 JSON에 아래 키도 함께 넣어라.\n"
            + _축_안내(메시지축)
        )

    응답 = client.chat.completions.create(
        model=설정["nvidia"]["model"],
        messages=[{"role": "user", "content": 프롬프트}],
        **_호출옵션(설정),
    )
    return _분류결과_검증(_JSON_파싱(응답.choices[0].message.content), 설정)


def 광고_분류(client, 이미지_경로, 광고주, 광고텍스트, 설정):
    """이미지에서 문구를 읽어 광고 텍스트에 합친 뒤 분류한다. 실패 시 예외를 발생시킨다."""
    # 광고텍스트는 이미 수집돼 있으므로 이미지는 보강재다. OCR이 실패해도 그 건을
    # 통째로 버리지 않고 텍스트만으로 분류한다 (무료 티어는 503/타임아웃이 잦다).
    try:
        이미지문구, 시각값 = _이미지_문구_읽기(client, 이미지_경로, 설정)
    except Exception as e:
        print(f"    (이미지 읽기 실패 - 텍스트만으로 분류: {type(e).__name__})")
        이미지문구, 시각값 = None, {}

    결과 = 광고_분류_텍스트전용(client, 광고주, _이미지문구_합치기(광고텍스트, 이미지문구), 설정)
    결과.update(시각값)   # 시각 축은 이미지를 본 쪽 판단을 그대로 쓴다
    return 결과
