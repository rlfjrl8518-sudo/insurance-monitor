"""Gemini API로 광고 이미지+텍스트를 분석해 소재유형/보종/후킹/요약으로 분류하는 모듈."""

import json

import google.generativeai as genai

분류_결과_키 = ["소재유형", "보종", "후킹", "요약"]

# 보종 분류 규칙 (소재명/광고 내용에 포함된 키워드 기반 우선순위 규칙)
보종_분류_규칙 = """- 보종: 광고의 핵심 보험 상품(소재명)을 아래 규칙에 따라 분류하세요. 반드시 우선순위 순서대로 적용하세요.

  1. 여성보험 (최우선): 여성, 유방암, 자궁, 난소, 여성질환, 여성건강, 임신, 출산 키워드 포함 시
     예) 여성암보험->여성보험, 여성간병보험->여성보험, 임신준비보험->여성보험, 출산플랜->여성보험, 3040여성보험->여성보험
  2. 태아보험: '태아' 키워드가 포함된 경우만 (임신/출산만 있으면 여성보험)
     예) 태아보험->태아보험, 태아플랜->태아보험, 우리아이태아보험->태아보험
  3. 어린이보험: 어린이, 자녀, 아이, 우리아이
     예) 어린이보험->어린이보험, 자녀보험->어린이보험
  4. 암보험: 암, 암진단, 암치료, 항암, 표적항암 (단, 여성 관련 키워드가 있으면 여성보험 우선)
     예) 암보험->암보험, 암진단비->암보험, 표적항암->암보험, 여성암보험->여성보험
  5. 간병인보험: 간병, 간병인, 치매, 요양, 장기요양 (단, 여성 관련 키워드가 있으면 여성보험 우선)
     예) 간병인보험->간병인보험, 치매보험->간병인보험, 여성간병보험->여성보험
  6. 운전자: 운전자, 교통사고, 벌금, 변호사선임, 사고처리
  7. 주택화재: 화재, 주택, 아파트, 누수, 동파, 상가, 재산, 배상책임
     예) 주택화재보험->주택화재, 누수피해->주택화재, 동파->주택화재
  8. 골프플랜: 골프, 홀인원
  9. 펫보험: 펫, 강아지, 고양이, 반려동물
  10. 자동차: 자동차, 차량, 자차, 자동차보험
  11. 보장분석: 보장분석, 보험점검, 보험진단, 보험리모델링, 리모델링
  12. 연령제한보험: 특정 연령을 타겟으로 하는 종합보험. 20대, 30대, 40대, 50대, 60대, 2030, 3040, 4050, 5060, 1040,
      새내기, 사회초년생, 가입연령, 연령, 나이 키워드 포함 시.
      단, 암/여성/간병 등 명확한 보종 키워드가 함께 있으면 해당 보종을 우선
      예) 3040종합보험->연령제한보험, 새내기보험->연령제한보험, 3040암보험->암보험, 3040여성보험->여성보험, 3040간병보험->간병인보험
  13. 종합보험: 위 카테고리에 해당하지 않는 일반 보험
      예) 한아름종합보험->종합보험, 값진인생보험->종합보험, 건강보험->종합보험, 종합보험->종합보험
  14. 위 규칙에 모두 해당하지 않으면 미분류

  최종 우선순위: 여성보험 > 태아보험 > 어린이보험 > 암보험 > 간병인보험 > 운전자 > 주택화재 > 골프플랜 > 펫보험 > 자동차 > 보장분석 > 연령제한보험 > 종합보험 > 미분류"""

확장자별_MIME = {
    ".jpg": "image/jpeg",
    ".jpeg": "image/jpeg",
    ".png": "image/png",
    ".webp": "image/webp",
}


def Gemini_모델_생성(설정):
    """config.json의 Gemini 설정으로 GenerativeModel 인스턴스를 생성한다.

    gRPC 전송 시 API 키 자격증명 플러그인이 "Illegal header value" 오류와 함께
    응답 없이 재시도를 반복하는 경우가 있어, REST 전송을 사용해 이를 회피한다.
    """
    genai.configure(api_key=설정["gemini"]["api_key"], transport="rest")
    return genai.GenerativeModel(설정["gemini"]["model"])


def 분류_프롬프트_생성(광고주, 광고텍스트, 설정):
    """config.json의 분류 기준을 반영한 Gemini 프롬프트를 생성한다."""
    분류기준 = 설정["classification"]
    소재유형_목록 = ", ".join(분류기준["소재유형"])
    후킹_목록 = ", ".join(분류기준["후킹"])

    return f"""당신은 손해보험사 메타(Facebook/Instagram) 광고 소재를 분석하는 마케팅 분석가입니다.
아래 광고 이미지와 광고 텍스트를 보고 광고를 분류해주세요.

[광고주] {광고주}
[광고 텍스트]
{광고텍스트}

다음 기준에 따라 분류하고, 반드시 아래 JSON 형식으로만 응답하세요.
다른 설명이나 마크다운 코드블록 없이 순수 JSON 객체만 출력하세요.

- 소재유형: 다음 중 하나를 선택 ({소재유형_목록})
  - 비갱신: 비갱신형 상품 또는 갱신 없음을 강조하는 소재
  - 특약: 특정 특약/보장 항목을 강조하는 소재
  - 브랜딩: 회사 이미지, 브랜드 인지도 제고 목적의 소재
{보종_분류_규칙}
- 후킹: 다음 중 광고에서 가장 강조하는 후킹 포인트 하나를 선택 ({후킹_목록})
  - 가격: 보험료, 가격 혜택 강조
  - 보장: 보장 범위, 보장 내용 강조
  - 긴급성: 한정 기간, 마감 임박 등 긴급성 강조
  - 가입편의: 간편 가입, 빠른 가입 절차 강조
  - 감성: 감성적 스토리, 가족/사랑 등 정서적 호소
- 요약: 광고 이미지와 텍스트를 종합한 핵심 내용 2~3문장 요약

응답 형식 예시:
{{"소재유형": "특약", "보종": "운전자", "후킹": "보장", "요약": "..."}}
"""


def 광고_분류(model, 이미지_경로, 광고주, 광고텍스트, 설정):
    """이미지 파일과 광고 텍스트를 Gemini에 전달하여 분류 결과(dict)를 반환한다.

    실패 시 None을 반환한다.
    """
    확장자 = "." + 이미지_경로.rsplit(".", 1)[-1].lower()
    mime_type = 확장자별_MIME.get(확장자, "image/jpeg")

    with open(이미지_경로, "rb") as f:
        이미지_바이트 = f.read()

    프롬프트 = 분류_프롬프트_생성(광고주, 광고텍스트, 설정)

    응답 = model.generate_content(
        [
            {"mime_type": mime_type, "data": 이미지_바이트},
            프롬프트,
        ],
        generation_config={"response_mime_type": "application/json"},
        # gemini-2.5-flash는 추론(thinking) 과정으로 응답이 길어질 수 있어 충분히 여유를 둔다.
        # 할당량 초과(429) 등 오류 시 SDK가 과도하게 재시도하는 것은 이 타임아웃으로 제한된다.
        request_options={"timeout": 60},
    )

    결과 = json.loads(응답.text)

    분류기준 = 설정["classification"]
    if 결과.get("소재유형") not in 분류기준["소재유형"]:
        결과["소재유형"] = 분류기준["소재유형"][-1]
    if 결과.get("보종") not in 분류기준["보종"]:
        결과["보종"] = "미분류" if "미분류" in 분류기준["보종"] else 분류기준["보종"][-1]
    if 결과.get("후킹") not in 분류기준["후킹"]:
        결과["후킹"] = 분류기준["후킹"][-1]
    결과["요약"] = 결과.get("요약", "")

    return 결과
